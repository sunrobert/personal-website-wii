# Wii Audio Assets — TODO

These files are expected at runtime but not yet present:

- `bgm.mp3` — Wii Menu theme, looped
- `hover.mp3` — channel hover SFX
- `select.mp3` — channel select SFX
- `back.mp3` — return SFX
- `startup.mp3` — power-on jingle

The audio system tolerates missing files (Howler logs a warning to the console).
Drop any `.mp3` into this folder with the names above and reload.

Sources commonly used by fan projects: `sounds-resource.com/wii/wiimenu/`.
